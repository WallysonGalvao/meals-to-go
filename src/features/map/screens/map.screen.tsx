import React, { useContext, useState, useEffect } from 'react';
import MapView from 'react-native-maps';

const MapScreen = (): JSX.Element => <MapView style={{ height: '100%' }} />;

export default MapScreen;
